[package]
name = "web"
version = "0.1.0"
edition = "2021"

[dependencies]
dioxus = { workspace = true, features = ["router"] }
server = { workspace = true }

dotenv = "0.15.0"
web-sys = "0.3.76"

[features]
default = []
web = ["dioxus/web"]
server = ["dioxus/server", "server/api"]
